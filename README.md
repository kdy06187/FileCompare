# (C# 코딩) FileCompare

## 개요
- C# 프로그래밍 학습

- 1줄 소개 : 사용자의 입력을 받아서 파일을 비교하는 프로그램

- 사용한 플랫폼 :
  - ```C#```, ```.NET Windows Forms```, ```Visual Studio 2026```, ```GitHub``` 
  
- 사용한 컨트롤 :  
    - ```Label, Button, ListView, TextBox, SplitContainer, Panel```
    
- 사용한 기술과 구현한 기능 :
  - **Windows Forms 앱 (C#)**: `Visual Studio`의 디자이너를 활용하여 직관적인 키오스크 UI(RadioButton, CheckBox) 
  - **레이아웃 속성 활용**: ```Padding```, ```Dock```,```Anchor```를 활용해서 컨트롤 배치 및 자동 조정
  - **ListView 활용** : ```ListView``` 컨트롤을 활용해서 파일 리스트를 표 형태로 표시
  - **Panel 활용** :  ```Panel``` 컨트롤을 활용하여 다른 컨트롤들을 하나로 묶어서 관리
  - **폴더 선택** : ```FolderBrowserDialog```을 활용해서 컴퓨터의 디렉터리 접근
  - **파일 비교** : ```FileInfo``` 클래스의 ```LastWriteTime``` 속성을 활용해서 파일의 마지막 수정 시간 비교
  - **파일 상태 관리** : ```enum```과 사용자 정의 클래스 ```FileItemData```를 활용해서 파일 상태 관리
  - **ListViewItem 태그 활용** : ```ListViewItem```의 ```Tag``` 속성을 활용해서 각 파일 아이템에 ```FileItemData``` 객체 연결
  - **디렉터리 클래스 활용** : ```Directory``` 클래스를 활용하여 폴더의 모든 파일과 하위폴더 불러오기
  
 
- 화면 구성 : 
  
  ![메인화면](img/Main.png)

## 실행 화면 (과제1)

- 과제1 코드의 실행 스크린샷

![과제1 스크린샷](img/Step1(1).png)
![과제1 스크린샷](img/Step1(4).png)
![과제1 스크린샷](img/Step1(2).png)
![과제1 스크린샷](img/Step1(3).png)


  - 과제 내용
    - 컨트롤 배치와 기본적인 속성 설정 
    - 컨트롤 이름 정하기
    - 레이아웃 속성 설정
    - 파일 불러오기
      
  - 구현 내용과 기능 설명
    - **UI 구성** : ```ListView```와 ```TextBox```등을 적절히 배치
    - **레이아웃 속성** : 레이아웃 속성을 설정하여 창 크기에 따라 컨트롤이 자동으로 조정되도록 설정
    - **폴더 선택** : 컴퓨터의 디렉터리 접근하여 폴더 선택
    - **ListView 속성** : ```ListView```속성을 설정하여 파일 리스트를 표 형태로 표시
 
 - - 사용한 기술과 구현한 기능
    - ```Padding```,```Anchor```, ```Dock```을 활용하여 레이아웃 설정
    - ```FolderBrowserDialog```을 활용해서 컴퓨터의 디렉터리 접근
    - ```ListView``` 컨트롤의 ```View```,```FullRowSelect```,```GridLines``` 속성을 활용해서 파일 리스트를 표 형태로 표시
    - ```EnumerateDirectories/Files``` 메서드를 활용해서 폴더의 모든 파일과 하위폴더 불러오기(```.Select()```와 ```.OrderBy()```로 정렬)
    - ```try catch finally```문을 활용해서 예외 처리 (예: 접근 권한이 없는 폴더)

## 실행 화면 (과제2)

- 과제2 코드의 실행 스크린샷
   ![과제2 스크린샷](img/Step2.png)


- 과제 내용
   - 폴더 선택 기능과 파일 리스트 기능 구현
   - 양쪽 폴더의 색상 구분 표시(동일 파일– 양쪽 모두 검은색, 다른 파일- New는 빨간색, Old는 회색, 단독 파일- 보라색)
      
- 구현 내용과 기능 설명
   - **폴더 구분 표시** : 폴더 상태 구분에 따라 색상 표시   

- 사용한 기술과 구현한 기능
   - ```enum```을 활용해서 폴더 상태 정의 (예: 동일, 최신, 과거, 단독)
   - 사용자 정의 클래스 ```FileItemData```을 활용해서 파일 정보와 상태 관리(마지막 수정시간, 상태)
   - ```ListViewItem```의 ```Tag``` 속성을 활용해서 각 파일 아이템에 ```FileItemData``` 객체 연결
   - ```ListView```의 ```ForeColor``` 속성을 활용해서 파일 상태에 따른 색상 표시
   
## 실행 화면 (과제3)

- 과제3 코드의 실행 스크린샷
    ![과제3 스크린샷](img/Step3.png)
    ![과제3 스크린샷](img/Step3(2).png)
    ![과제3 스크린샷](img/Step3(3).png)

 - 과제 내용
    - 양쪽 폴더 사이에서 파일의 복사 기능 구현

 
 - 구현 내용과 기능 설명
    - 선택한 파일을 반대쪽 폴더로 복사
    - 수정된 날짜 정보를 확인해서 "확인"받아 진행여부 결정

- 사용한 기술과 구현한 기능
    - ```Path.Combine``` 메서드를 활용해서 파일 복사 경로 생성 후 ```File.Copy``` 메서드로 파일 복사
    - ```MessageBox.Show``` 메서드를 활용해서 사용자에게 파일 복사 여부 확인 메시지 표시

## 실행 화면 (과제4)

 - 과제4 코드의 실행 스크린샷
    ![과제4 스크린샷](img/Step4(1).png)
    ![과제4 스크린샷](img/Step4(2).png)
   

- 과제 내용
   - 하위폴더에 대해서 한 방에 비교, 복사가 가능하도록 개선

- 구현 내용과 기능 설명
   - 하위폴더를 하나의 파일처럼 처리
   - 적절하게 색상 표시
   - 복사버튼 누르면 하위폴더의 모든 내용(파일과 하위폴더 포함) 처리

- 사용한 기술과 구현한 기능
    - ```Directory.GetFiles```와 ```Directory.GetDirectories``` 메서드를 활용해서 하위폴더의 모든 내용 불러오기
    - 하위 폴더도 하나의 아이템으로 처리하기 위해 ```FileItemData``` 클래스에 ```IsDirectory``` 속성 추가
    - 하위 폴더의 모든 내용을 재귀적으로 처리
